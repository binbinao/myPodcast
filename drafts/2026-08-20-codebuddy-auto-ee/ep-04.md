---
title: 汽车 E&E 工程的 AI 助手化：CodeBuddy 脚手架工程全景 · 第 4 集 · AUTOSAR 架构：标准化开发范式与 AI 助手的融合
description: ''
format: duo
series: 汽车 E&E 工程的 AI 助手化：CodeBuddy 脚手架工程全景
series_slug: codebuddy-auto-ee
episode: 4
total: 9
chapter: 第 4 集 · AUTOSAR 架构：标准化开发范式与 AI 助手的融合
voice: host=audiobook_male_1 / guest=audiobook_female_1
host_voice: audiobook_male_1
guest_voice: audiobook_female_1
split_strategy: by_h2
source: raw/2026-08-20-codebuddy-auto-ee.md
ai_stage: generated
---

[host]: 大家好，欢迎来到本期节目。今天我们要聊一个在汽车电子行业特别重要的话题——AUTOSAR。可能很多朋友听说过，但具体是什么可能还不太清楚。咱们今天就把它讲透。

[guest]: 好的。AUTOSAR其实是Automotive Open System Architecture的缩写，翻成中文就是汽车开放系统架构。这几年在汽车行业特别火，已经成为最核心的软件架构标准了。

[host]: 那它到底解决了什么问题呢？

[guest]: 你想啊，现在一辆车里有几十甚至上百个ECU，每个供应商都有自己的开发方式。这样搞下去，整个生态就很混乱。AUTOSAR就是来制定统一的规范，让不同供应商的软件能够互相通信、互相替换。它不仅是一套规范，更是一种全新的软件开发范式，深刻影响着汽车E&E工程师的日常工作方式。

[host]: 明白了，相当于给整个行业定了个"普通话"。那它的架构是怎么设计的？

[guest]: AUTOSAR采用的是经典的分层架构，从底层到顶层依次是：微控制器抽象层MCAL、基础软件层BSW、运行时环境RTE和应用层。咱们一个个来说。

[host]: 好，先从最底层开始。

[guest]: MCAL是架构的最底层，直接和硬件打交道。它包含驱动程序、接口、操作系统的低层抽象。不过工程师一般不直接写代码，而是通过配置工具生成MCAL代码。

[host]: 也就是说，这层主要是帮工程师屏蔽硬件细节？

[guest]: 对，完全正确。工程师用配置工具点点鼠标就能生成底层代码，不用去抠具体的寄存器配置。

[host]: 那再往上是BSW？

[guest]: 对，BSW是AUTOSAR的核心，包含好几个功能模块组。服务层提供操作系统、内存管理、通信管理这些基础服务。ECU抽象层封装ECU级别的硬件访问。复杂驱动处理那些无法标准化的特殊硬件。通信驱动就是CAN、LIN、FlexRay、以太网这些总线驱动。

[host]: 内容挺丰富的。那再往上是RTE？

[guest]: RTE是AUTOSAR的一个很巧妙的设计。它被称为"虚拟功能总线"，负责应用软件组件之间的通信，以及应用软件和基础软件之间的交互。SWC之间不直接通信，而是通过RTE来抽象。这种设计实现了应用软件与基础软件的解耦——这点特别重要。

[host]: 解耦的好处是什么？

[guest]: 好处大了。比如你换一个供应商的硬件，或者升级底层软件，上层的应用代码基本不用动。因为应用层只关心RTE提供的接口，不关心底层怎么实现。

[host]: 这确实很实用。最顶层就是应用层了吧？

[guest]: 对，应用层包含多个软件组件SWC。每个SWC是独立的功能单元，通过端口发送和接收数据。SWC之间不直接通信，都通过RTE来中转。

[host]: 明白了，整个架构层次很清晰。那开发流程和传统软件开发有什么不同？

[guest]: 区别挺大的。第一个阶段是系统配置阶段，要定义系统约束、ECU网络拓扑、信号映射。输出是系统描述文件，包含所有ECU的接口和连接关系。

[host]: 听起来像画图纸。

[guest]: 有点那个意思。第二个阶段是ECU配置阶段，针对单个ECU进行配置，包括BSW模块参数、RTE生成配置、通信栈配置等。主流配置工具有Vector DaVinci、Elektrobit EB tresos Studio、KIT AUTOSAR Builder这些。

[host]: 第三个阶段是代码生成？

[guest]: 对，配置工具根据配置生成BSW代码、RTE代码、接口代码。生成的代码遵循AUTOSAR标准接口，确保互操作性。第四个阶段是应用开发阶段，开发SWC应用代码。SWC可以使用AUTOSAR接口调用BSW服务，或者用标准接口和其他SWC通信。最后是集成测试阶段，在HIL或SIL测试平台上验证系统功能。

[host]: 流程确实很规范。那CodeBuddy在这种场景下能帮上什么忙？

[guest]: CodeBuddy可以在多个层面支持AUTOSAR开发。比如RTE接口代码生成，当SWC的端口接口定义完成后，可以根据ARXML接口描述生成Sender-Receiver或Client-Server通信代码。还有BSW模块配置建议，配置CAN通信栈时，可以建议合理的仲裁位速率、采样点位置、过滤器配置这些参数。另外也可以预置各类AUTOSAR SWC模板，包括传感器组件、执行器组件、算法组件等，帮助快速创建符合规范的应用代码结构。

[host]: 这些功能都挺实用的。那如果要构建脚手架工程，你有什么建议？

[guest]: 首先可以建立AUTOSAR配置模板库，预置BSW模块配置模板、RTE配置模板、SWC组件模板这些。其次可以做一个接口契约生成器，基于功能需求自动生成SWC的ARXML接口描述，定义端口接口、数据元素、操作、参数这些。

[host]: 还需要什么？

[guest]: 代码与配置同步检查也很重要。AUTOSAR开发中，代码实现必须与配置保持一致。CodeBuddy可以实现头文件接口与ARXML接口的一致性检查，运行实体与代码函数的映射验证，以及数据一致性的交叉检查。

[host]: 代码风格也要管？

[guest]: 对，AUTOSAR对代码有特定的命名规范和结构要求。CodeBuddy可以验证接口函数命名、检查模块结构、验证错误代码定义是否与规范一致。

[host]: 最后还要考虑和现有工具链的集成吧？

[guest]: 没错。CodeBuddy需要与主流AUTOSAR工具链集成，包括Vector DaVinci、EB tresos Studio、AUTOSAR Builder这些。通过插件机制，可以直接读取和修改这些工具的配置数据，实现深度集成。

[host]: 好的，AUTOSAR的内容咱们就聊到这儿。感谢嘉宾的分享！

[guest]: 不客气！

[host]: 了解了AUTOSAR架构之后，让我们转向另一个重要标准——SOTIF，它是ISO 26262的重要补充，专注于预期功能安全。
